# Next.js no Netlify usa o adaptador OpenNext automaticamente.
# Variável obrigatória para as rotas /api: GOOGLE_AI_API_KEY (Site → Environment variables)

[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
