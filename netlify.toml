# Next.js no Netlify usa o adaptador OpenNext automaticamente.
# Rotas /api de geração: OPENROUTER_API_KEY e/ou GOOGLE_AI_API_KEY (Site → Environment variables)

[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
